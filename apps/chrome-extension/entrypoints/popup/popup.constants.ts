/** Popup shell width in CSS pixels. */
export const POPUP_WIDTH = 420;

/** Portrait popup height for a 3:4 frame (width:height = 3:4). */
export const POPUP_HEIGHT = Math.round((POPUP_WIDTH * 4) / 3);
